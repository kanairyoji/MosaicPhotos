# 顔認識の精度記録（ベンチマーク台帳）

顔認識パイプラインの**精度に関する現状値・パラメーター・計測結果**を集約する台帳。
チューニングの判断は本台帳の**データセット計測**で行う（体感・個別事例では決めない）。
計測方法は ADR-55、パイプラインの経緯は ADR-45〜54 を参照。

## 計測方法

```bash
# データセット取得（FG-NET・初回のみ）
scripts/fetch_face_eval_datasets.sh

# 計測（シミュレータ・実機不要。埋め込みはキャッシュされ2回目以降は数分）
xcodebuild test -project MosaicPhotos.xcodeproj -scheme MosaicPhotos \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -parallel-testing-enabled NO \
  -only-testing:MosaicPhotosTests/FaceAccuracyEvalTests
```

出力は `FACEEVAL:` 行。指標の定義は `FaceEvalMetrics.swift`（B-Cubed / ペア一致 / TAR@FAR）。

## 現行パラメーター（v4 パイプライン）

| パラメーター | 現行値 | 定義場所 |
|---|---|---|
| クラスタリングしきい値（既定） | 0.50（ADR-58・サイズ適応併用） | `FaceStore.clusterThreshold` |
| マージンゲート幅 | 0.05（1位と2位の差がこれ未満なら合流しない） | `FaceStore.assignMargin` |
| サイズ適応マージン最大 | 0.10（小/新クラスタの合流を厳しく・成熟11で0） | `FaceStore.sizeAdaptiveMarginMax` |
| しきい値校正の可動域 | 0.35〜0.70 | `FaceCalibration.clampRange` |
| 校正の最小サンプル数 | 正負各 8 | `FaceCalibration.minSamples` |
| 品質フロア（未満はクラスタ不参加） | 0.40 | `FaceStore.qualityFloor` |
| 検出信頼度の下限 | 0.80 | `FaceQualityGate.minDetectionConfidence` |
| 最小顔サイズ（正規化比率） | ローカル 0.05 / クラウド 0.15 | `FaceQualityGate.minFaceSide` |
| 最小顔サイズ（絶対ピクセル辺） | 48px | `FaceQualityGate.minFacePixels` |
| 横顔キャップ（\|yaw\|） | ≥30° → 品質 0.2 | `FaceQualityGate.yawLimit/profileCap` |
| 傾きキャップ（\|roll\|） | ≥45° → 品質 0.2 | `FaceQualityGate.rollLimit` |
| 目閉じ減衰 | ×0.6 | `FaceQualityGate.eyesClosedFactor` |
| ぼけ（ラプラシアン分散・64px 基準） | <25 キャップ / <60 ×0.7 | `FaceQualityGate.blurHardFloor/blurSoftFloor` |
| 露出（平均輝度） | <20 or >235 キャップ / <40 or >215 ×0.6 | `FaceQualityGate.luma*` |
| クロップ再検証の最小占有 | 幅 25% | `FaceQualityGate.cropVerifyMinSide` |
| 埋め込み | facenet InceptionResnetV1/VGGFace2・512 次元・アライメント＋マルチクロップ 3 平均 | `FacePerceptionAdapter` |
| 処理解像度 | ローカル 1024px / クラウドはキャッシュサムネ | 同上 |
| 共起 notSame | 同一写真 3 回以上で別人扱い | `FaceStore.coOccurrenceNotSame` |
| パイプライン版数 | v4 | `PeopleEngine.faceScanVersion` |

## 計測ログ

### 2026-08-01 — v4 ベースライン（FG-NET・1,002 枚・82 人・0〜69 歳）

**検出（顔を見つける）: 良好**
- 顔採用率: **99.6%**（998/1002）。年齢別: 0-2歳 98% / 3-5歳 100% / 6-12歳 100% / 13-19歳 100% / 20歳+ 100%
- ゲートによる正当な顔の過剰棄却は無し

**識別（同一人物と分かる）: 弱い（現状の主課題）**
- 検証: 同一ペア平均類似度 0.550 / 別人ペア 0.151・**TAR@FAR1% = 48.1%**・TAR@FAR0.1% = 24.3%・最良 F1 しきい値 0.64
- 同一人物の年齢差別 平均類似度: **0-2年 0.718 / 3-5年 0.661 / 6-10年 0.558 / 11-20年 0.440 / 21年+ 0.400**
  （年齢差 10 年超で現行しきい値 0.45 を平均が下回る＝成長期分裂の定量確認）
- 別人×両者 12 歳以下（兄弟の代理）: 平均 0.294

**クラスタリングしきい値スイープ（B-Cubed）**

| thr | 精度 P | 再現率 R | F1 | ペア F1 | クラスタ数 |
|---|---|---|---|---|---|
| 0.35 | 0.249 | 0.485 | 0.329 | 0.096 | 112 |
| 0.40 | 0.299 | 0.466 | 0.364 | 0.106 | 124 |
| 0.45（当時の既定） | 0.420 | 0.472 | 0.444 | 0.163 | 144 |
| 0.50 | 0.528 | 0.474 | 0.500 | 0.190 | 169 |
| 0.55 | 0.610 | 0.468 | 0.530 | 0.258 | 196 |
| **0.60（現行既定）** | 0.721 | 0.435 | **0.542** | 0.296 | 248 |
| 0.65 | 0.822 | 0.399 | 0.538 | 0.365 | 322 |
| 0.70 | 0.907 | 0.342 | 0.497 | 0.360 | 416 |

### 2026-08-01 — 改善バリアント計測と採否（ADR-56）

**偽陽性（negatives・顔なし 18 枚＝風景/物/猫犬）: 誤採用 0 枚・0 顔（0.0%）** — ゲート群
（信頼度・二段検出・ぼけ/露出/サイズ）が偽陽性を完全に抑止。

**P1 しきい値 0.45→0.60: 採用** — F1 0.444→0.542・純度 0.42→0.72（上のスイープ表）。
`FaceStore.clusterThreshold = 0.60` に変更（校正の可動域 0.35〜0.70 は維持・
しきい値変更で夜間再クラスタが自動発火）。

**P2 自動プロトタイプ（K=5・オンライン最遠点）: 不採用** — 全しきい値でベースラインより悪化
（thr 0.60: F1 0.523 vs 0.542・純度 0.642 vs 0.721）。クラスタ内の単結合的連鎖で混入が増える。
実装は評価用に残置（本番は無効＝ユーザー確認アンカーのみ従来どおり使用）。

| thr | base F1 | proto F1 |
|---|---|---|
| 0.45 | 0.444 | 0.336 |
| 0.55 | 0.530 | 0.510 |
| 0.60 | **0.542** | 0.523 |
| 0.70 | 0.497 | 0.514 |

**P3 連鎖統合（推移的クラスタ統合）: 不採用** — proto 上でも base 上でも悪化を確認
（base thr=0.60 chain=0.60 が連鎖系の最良で F1 0.513 < 単純 0.60 の 0.542。chain を下げるほど
純度が 0.10 台まで崩壊）。50 万ペアの裾では別人の子供同士にも高類似ペアが存在し、推移的統合が
雪崩式の誤統合を起こす。自動統合は危険で、**統合は人間の確認（レビュー A1・帯域 [thr−0.10, 1.0]）
に限定**するのが正解という結論。実装は評価用に残置。

**P5 モデル換装の再調査: 保留継続** — ArcFace 級で許諾的ライセンスの学習済み重みは現状存在せず
（コード MIT でも重みは非商用限定が通例）。商用可の代替（dlib 系）は精度が現行 facenet 同等で
換装の妙味なし。選択肢は商用モデルライセンスの取得か、合成データ学習モデルの成熟待ち。

**未計測**: 実ライブラリ分布での絶対値（own/ は使わない方針のため、FG-NET の相対比較で判断）。

### 2026-08-01 — 系統1 バリアント計測と採用（ADR-57）＋ LFW 追加

**LFW（成人・易しいケース・4,862 枚/901 人・deepfunneled 間引き）を追加**。
検出 100.0%（4861/4862）・TAR@FAR1% = **98.6%** / TAR@FAR0.1% = 94.0%・最良 F1 しきい値 0.66。
→ FG-NET（TAR@FAR1% 48.1%）との対比で「facenet は同年代の大人には十分強く、
弱点は年齢変化に集中している」ことを確認。リグレッション番犬として常設。

**クラスタリングバリアント（B-Cubed F1・両データセット）**

| 構成 | FG-NET | LFW |
|---|---|---|
| base thr=0.55 | 0.530 | 0.742 |
| base thr=0.60（旧採用） | 0.542 | 0.793 |
| **marginGated thr=0.55 m=0.05（採用）** | **0.585** | 0.775 |
| marginGated thr=0.60 m=0.05 | 0.562 | 0.796 |
| marginGated m=0.10 | 0.484〜0.490 | —（網羅崩壊で不採用） |
| twoStage（二段階＋比率テスト） | 0.530〜0.561 | —（劣後で不採用） |
| median thr=0.60（中央値重心） | 0.563 | 0.807 |

**採用: マージンゲート付き貪欲（thr=0.55・margin=0.05）** — 難所（FG-NET）で最大の
F1 0.585（旧採用比 +8%・純度 0.849）。LFW は base0.60 比 −0.018 の微差だが、
実ライブラリの難所（兄弟＋成長期）は FG-NET 型のため難所側を優先。旧本番（0.45）比では
FG-NET +32% / LFW +36% の両面改善。本番配線: `FaceClustering.assignMargin`
（逐次スキャン・夜間再クラスタ共通）＋ `FaceStore.clusterThreshold=0.55` / `assignMargin=0.05`。

**次点候補（未採用・将来の組み合わせ余地）**: 中央値重心（LFW 最良 0.807・FG-NET 0.563）。
マージンゲートとの併用は未計測。

**高速化**: `FaceClustering.dot` を vDSP 化（照合が夜間再クラスタとハーネスの支配項のため）。

### 2026-08-01 — サイズ適応マージンの採用（ADR-58）

**採用: サイズ適応マージン**＝小さい/新しいクラスタほど合流の実効しきい値を上げる
（count=1 で最大上乗せ・成熟サイズ11 で 0 に線形減衰）。成長期に分岐した小クラスタが
兄弟を吸い込むのを防ぎ、確立した本人には寛容にする。本番構成:
`clusterThreshold=0.50`＋`assignMargin=0.05`＋`sizeAdaptiveMarginMax=0.10`。

| 構成 | FG-NET F1 | FG-NET 純度 | LFW F1 | LFW 純度 |
|---|---|---|---|---|
| current（thr0.55・margin0.05） | 0.585 | 0.849 | 0.775 | 0.867 |
| **sizeAdapt thr0.50 m0.05 smax0.10（採用）** | **0.589** | 0.879 | **0.785** | 0.902 |
| sizeAdapt smax0.05 | 0.587 | 0.849 | 0.777 | 0.871 |
| sizeAdapt smax0.15 | 0.551（網羅低下） | 0.889 | — | — |
| sizeOnly（固定マージンなし） | 0.520 | 0.690 | — | — |

両データセットで純度・F1 とも改善（FG-NET +0.004/+0.030・LFW +0.010/+0.035）。
smax0.15 は網羅が崩れ、固定マージンとの併用が必須（sizeOnly は劣化）。

### 2026-08-01 — 純度の事後検査（外れ値除去）: 計測の結果 不採用（ADR-59）

現行本番構成の後段に「クラスタ内メンバーのロバスト重心（中央値）からの距離が
中央値距離×factor かつ絶対 0.30 超の顔を外れ値として抜き単独化」を重ねて計測。

| 構成 | FG-NET F1 | FG-NET 純度 | LFW F1 | LFW 純度 |
|---|---|---|---|---|
| prod（外れ値除去なし） | 0.589 | 0.879 | 0.785 | 0.902 |
| prune factor=1.5 | 0.566 | 0.900 | 0.789 | 0.939 |
| prune factor=1.8 | 0.580 | 0.888 | 0.789 | 0.933 |
| prune factor=2.2 | 0.584 | 0.885 | 0.789 | 0.926 |

**不採用**: 純度は上がるが FG-NET（成長・主眼）では総合 F1 が全設定で悪化。外れ値除去は
「重心から遠い顔＝別人混入」を前提とするが、成長データでは「遠い顔＝同一人物の別年齢」の
ことが多く、正当な顔を弾いて網羅が落ちる。LFW（同年代）では +0.004 の微改善に留まる。
主眼（成長期＋兄弟）で悪化するため既定不採用（ADR-56 の難所優先の論理）。評価実装
`FaceClusteringVariants.pruneOutliers` は将来参照用に残置。

### 2026-08-01 — 年齢を使ったクラスタリング: 計測の結果 不採用（ADR-60）

FG-NET の正解年齢ラベルを「年齢推定が完璧だった場合の入力」として使い、上限効果を計測。

| 構成 | 純度 | 網羅 | B-Cubed F1 |
|---|---|---|---|
| prod（年齢不使用・現行本番） | 0.879 | 0.443 | 0.589 |
| X-1 年齢層で分類→各層内クラスタ | 0.879 | **0.252** | 0.392 |
| X-2 年齢連結（年齢連続クラスタを統合） | **0.097〜0.110** | 0.85 | 0.174〜0.194 |

**不採用（年齢を完璧に知っていても改善しない）**:
- **X-1（年齢で分ける）**: 純度は保つが**網羅が半減**（同一人物が年齢帯をまたいで別クラスタに分裂）。
  ＝欲しい「年齢差を乗り越えて繋ぐ」の逆。
- **X-2（年齢で繋ぐ）**: **純度崩壊**（同年代の別人を大量に誤統合＝P3 と同じ雪崩。年齢制約は
  同年代の別人を区別できない）。
- 結論: 年齢は「同一人物か別人か」を分ける力をほぼ持たない（同年齢の別人は無数・同一人物は
  年齢をまたぐ）。**完璧な年齢でこれなので、誤差のある推定モデルを同梱する価値はない**。
  資料の示唆（高度な写真アプリも年齢を人物クラスタリングには使わず関係推論等に使う）と一致。
  評価はハーネス内のみ（本番コード変更なし）。

### 2026-08-01 — 2 階層人物モデル（人物=複数クラスタの束）の検証: 識別では融合に優位なし（ADR-61）

「同一人物が複数グループになることを許容し（成長帯別など）、上位の人物で束ねる」案を
**検証可能な核（純ロジック `FacePersonGrouping` ＋評価）**として実装。UI/データモデルに進む前に
「融合（人物=1 重心）vs 2 階層（人物=複数クラスタ・帰属は最大類似度）」の人物識別精度を FG-NET で計測。

| 分割 | 融合（1 重心） | 2 階層（複数クラスタ） |
|---|---|---|
| 年齢混在（実運用に近い・クエリ169） | **95.9%** | 93.5% |
| 時系列（若→成長後・最も厳しい・クエリ236） | **89.0%** | 88.1% |

**UI 実装は保留（識別上の優位が実証されず）**:
- 当初仮説「融合すると重心が壊れる」は実データで成り立たず、融合は十分頑健。成長差程度は
  平均重心がカバーする。
- 逆に 2 階層は、人物あたりの顔が限られると時期別クラスタが小さくノイジーになり、
  最大類似度での帰属が別人の小クラスタへの誤帰属を増やす（識別率がむしろ下がる）。
- 含意: 分裂クラスタは現状の「統合（融合）」で束ねれば識別は落ちない＝2 階層の複雑な
  データモデルを入れる技術的メリットは（識別の観点では）薄い。純ロジック `FacePersonGrouping` と
  ハーネス評価は将来の再検証資産として残す（本番未接続）。

### 2026-08-01 — 2 階層の分け方を再検証: 少数の時期帯分割は融合を上回る（ADR-61 追補）

前回（自動クラスタリングで平均 2.7 個に細分割）は融合に負けたが、**分け方を「少数の年齢帯固定分割」に
変えると逆転**する。人物識別精度（FG-NET・帯は登録顔の年齢で分け各帯の重心を代表に）:

| 分け方 | 通常時(実運用) | 成長を跨ぐ(時系列) |
|---|---|---|
| 融合（人物=1 重心） | 95.9% | 89.0% |
| 自動クラスタリング（平均2.7） | 93.5% | 88.1% |
| 帯2（<5/≥5・平均1.9） | 95.3% | 91.1% |
| **帯3（<3/3-10/≥10・平均2.6）** | 94.7% | **92.8%** |
| 帯4（Baby/Child/Young/Adult・平均3.3） | 95.9% | 92.4% |
| 帯5（<1/<2/<4/<6/≥6・平均3.2） | 95.9% | 90.3% |

**結論の修正**: 2 階層が無効なのでなく、**細分割（自動）が悪く、少数（2〜3 帯）の時期分割が有効**。
成長を跨ぐ識別で融合を +3.8pt 上回り（帯3）、通常時は融合と同等＝ほぼ上位互換。細かくしすぎ
（帯5）ると自動クラスタリングと同じノイジー化で落ちる＝最適は 3 帯前後。
**実装は年齢推定 API 不要——撮影日で代用可能**（子供は撮影日≒年齢・EXIF で確実。今回の年齢帯結果が
撮影日帯分割の効果を示す）。次段: 撮影日帯で人物内をグループ化する 2 階層モデルの実装を検討。

## 運用ルール

- パイプライン・パラメーター変更時は本ハーネスで**変更前後を計測**し、この台帳に追記する。
- 判断はデータセット計測で行う（自前写真はデータセットに入れない方針）。
- FG-NET は相対比較用（モデル間・変更前後）。絶対値は分布差があることに留意。
