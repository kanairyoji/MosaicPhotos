# Xcode
xcuserdata/
*.xcuserstate
*.xcworkspace/xcuserdata/
DerivedData/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# Swift Package Manager
.build/
.swiftpm/
*.resolved

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Claude Code session data
.claude/

# Credentials / secrets
*.env
secrets/
# Dropbox app key — copy DropboxSecrets.swift.example to DropboxSecrets.swift and fill it in.
MosaicPhotos/DropboxSecrets.swift

# MobileCLIP: build venv/checkpoint (1.3GB) and generated on-device models (193MB).
# Regenerate locally with scripts/build_mobileclip.sh (not committed; license + size).
.mobileclip_build/
MosaicPhotos/MobileCLIP/

# Face model (people clustering): build venv and generated Core ML model.
# Regenerate locally with scripts/build_facenet.sh (not committed; size).
.facenet_build/
MosaicPhotos/FaceModel/

# VLM (photo captions for AI albums): build venv/checkpoint (~2GB) and generated models (~450MB).
# Regenerate locally with scripts/build_smolvlm.sh（現行・視覚のみINT8）/ build_florence.sh（撤回・参考）.
.smolvlm_build/
.vlmbench_flor/
MosaicPhotos/VLM/

# CLIP model comparison / quantization work dir (bench models). Regenerate with scripts/bench_clip.py etc.
.clipbench/

# VLM quantization work dir (bench models). Regenerate with scripts.
.vlm_q/

# SmolVLM-500M conversion work dir (models). Regenerate with build_smolvlm.sh.
.smolvlm500/
